<h2>Logg inn</h2>
<br>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email %>
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'text email' %>
  </div><br>

  <div class="field">
    <%= f.label :passord %>
    <%= f.password_field :password, autocomplete: "current-password", class: 'text email' %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :husk_meg %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Logg inn", class: 'buy-button' %>
  </div>
<% end %>

<%= render "members/shared/links" %>