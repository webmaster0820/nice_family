<h2 style="padding-top;padding-left: 50px;">Opprett nivå</h2>

<div class="page-card">
<%= form_with(model: course_niveau, local: true) do |form| %>
  <% if course_niveau.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_niveau.errors.count, "error") %> prohibited this course_niveau from being saved:</h2>

      <ul>
      <% course_niveau.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :navn %><br>
    <%= form.text_field :name %><br><br>
  </div>

  <div class="field">
    <%= form.label :beskrivelse %> (valgfri)<br>
    <%= form.text_area :description %><br><br>
  </div>

  <div class="actions">
    <%= form.submit "Opprett nivå", class: 'buy-button' %>
  </div>
<% end %>
</div>